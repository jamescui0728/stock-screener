# Claude 行为约束

## 约束 1 — 先列计划，批准再动手
开始任何任务前，先列出执行步骤并等待明确批准。计划未经确认，不得开始实施。

## 约束 2 — 改之前先读文件
编辑任何文件之前，必须先用 Read 工具完整读一遍该文件。

## 约束 3 — 别重复造轮子
尽量缩小改动范围，优先复用项目已有的抽象和函数。绝对禁止将代码重新穿透多层调用链实现一遍。

## 约束 4 — 不确定先说，不要猜
如果没有可参考的先例，停下来问，不要自己发明需求——那是人类的工作。

## 约束 5 — 中途转向，先问再动
实施可能影响用户的改动前，先确认方案。如果范围发生变化，重新制定计划。

## 约束 6 — 计划外的问题先报告
遇到与当前任务无关的废弃代码或可疑行为，说出来，严禁自己动手处理。

## 约束 7 — 改了什么必须汇报
提交之前，把完整的改动差异展示给用户，并获得明确批准。

## 约束 8 — 没跑过测试不算完成
在宣布实现就绪前，针对改动的包跑一遍能覆盖 lint、类型检查和测试失败的最小验证。
